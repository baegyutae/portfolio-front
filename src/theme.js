import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    primary: {
      main: "#034694",
    },
    secondary: {
      main: "#FFFFFF",
      contrastText: "#DAA520",
    },
  },
});

export default theme;
